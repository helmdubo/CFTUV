# Файлы без изменений

Следующие файлы остаются точно такими же, как в текущей версии проекта:

- `analysis.py` — без изменений
- `debug.py` — без изменений  
- `solve.py` — без изменений
- `constants.py` — без изменений

Скопируйте их из текущего `cftuv/` каталога.

## Удалённые файлы

- `Hotspot_UV_v2_5_26.py` — мёртвый legacy монолит. Можно архивировать или удалить.
  Никакой модуль больше не импортирует из него.

## Новые / изменённые файлы

- `CLAUDE.md` — обновлён (глоссарий, chain-first principle)
- `docs/cftuv_architecture_v2.0.md` — новый (заменяет v1.4)
- `model.py` — обновлён (добавлен ScaffoldSegment)
- `operators.py` — **НОВЫЙ** (извлечён из монолита)
- `__init__.py` — обновлён (импорт из operators.py, version bump 2.6.0)

# ROADMAP — сведение к одному decal-движку

Цель: остаётся один движок — envelope-ядро. `PATCH_VORONOI` и `RAIL_PLANAR`
удаляются. Не флаг-день, а последовательность с проверяемыми воротами.

Новый репозиторий не создаётся. Извлекаемость ядра уже доказана: job
`kernel-extraction-readiness` копирует `kernel/` в пустой репозиторий, собирает
wheel и прогоняет 227 тестов в изолированном venv. Чистый репозиторий генерируется
на каждом коммите — остаётся довести перевёртывание отношения: ядро становится
продуктом, аддон — тонким адаптером.

Легаси-код при этом является спецификацией нового движка: он знает про вырожденные
случаи вашей реальной геометрии то, что уже оплачено. Начав с нуля, эти случаи
пришлось бы переоткрывать в поле по одному.

---

## Фаза 0 — заморозка и уборка ✅

- Удалены `cftuv/band_operator.py` (447 строк, сирота), корневой
  `Hotspot_UV_v2_5_19.py`, `.tmp_review/` — 8981 строка мёртвого кода.
- `tests/test_architecture.py`: импортные стены, запрет сирот, бюджеты размера
  с храповиком, заморозка легаси, контроль роста бинарников.
- `AGENTS.md`: 415 → 145 строк. Правила переведены в исполняемую форму.
- `DECISIONS.md`: журнал решений вместо per-session handoff-документов.
- `pytest.ini`: чистый клон зелёный (`-m "not atlas_frozen"`, `--strict-markers`).
- `.gitattributes`: новые PNG/blend идут через LFS.

Ничего из этого не трогает геометрию.

---

## Фаза 1 — критерии приёмки ✅ (узкая версия)

`ACCEPTANCE.md` заполнен владельцем и намеренно узок: сцена `E:\testscene.blend`
(внешняя, в репозиторий не кладётся), меш `building.002`, рёбра-цепочки 1–36.
Предмет приёмки сейчас — debug-режим с отображением элементов Envelope и их
coverage; drag-режим нужен для всех мешей.

Кейсы добавляются по мере усложнения, а не декларируются авансом. Подробный
контекст проверки владелец задаёт во время тестирования с исполнителем.

`MITER`/`BEVEL` в envelope не переносятся — их заменяет динамический профиль от
reflex angle. Чеклист паритета не должен их содержать.

---

## Фаза 2 — производительность до паритета ✅ цель достигнута

**Полевой замер (Blender 4.3.2, `building.002`, все seam-цепочки, 13 доменов):
870 с → 24 с, ускорение 36×.** Цель шага была ≤ 30 с.

Оценка «примерно 340–380 с» по прогону тестового набора ядра оказалась
сильно пессимистичной: полевая нагрузка была куда плотнее забита
`sympy.factor` на рациональных, чем тесты. Урок на будущее — **мерить в поле,
а не экстраполировать по тестам**.

Остаток по стадиям: `RAW_UNION` 10.1 с, `INTERACTION` 9.0 с,
`DOMAIN_CLIP` 3.6 с. Два «тяжёлых» домена из тринадцати дают ~60% всего
времени (по 3.3 с на RAW_UNION против 0.2 с у остальных) — там и лежит
следующий резерв, если понадобится.

Исходный профиль (`test_session_d_resolved_coverage.py`, 261.7 с):

| Функция | Вызовов | Cumtime | Доля |
|---|---:|---:|---:|
| `planar_types.exact_sign` | 1 334 536 | 134.6 с | 51% |
| `sympy.polytools.factor` | 3 491 878 | 104.9 с | 40% |
| `ExactScalar.from_value` | 1 572 624 | 81.7 с | — |
| `arrangement._point_in_region` | 19 876 | 117.3 с | — |

Корень: `ExactScalar` хранит число **строкой** `srepr`, и каждая операция идёт по
циклу `строка → sympify → вычисление → cancel → factor → srepr → строка`.

### Шаг 1 — рациональный быстрый путь ✅

Замерено: **99.8%** вызовов `exact_sign` и **100%** вызовов `_canonical_expr`
приходят с обычными рациональными, где `factor(cancel(x))` возвращает ровно `x`.
Введён `exact_normalize()`, все 38 прямых `sp.factor(...)` в восьми модулях
заменены на него. Эквивалентность доказана побитово на 328k реальных вызовов
и закреплена `kernel/tests/test_exact_numeric_fast_path.py`.

Счётчик `SYMBOLIC_FALLBACK_COUNTS` показывает, когда в предикаты протекают
радикалы: на рациональном корпусе он остаётся нулевым, и это проверяется тестом.

### Шаг 1б — не вычислять ориентацию там, где она не используется ✅

В `_point_in_loop` знак `cross` считался для каждого сегмента петли, но читался
только под `upward`/`downward`. Замерено на реальных петлях: луч пересекают
**19.8%** сегментов, то есть четыре из пяти точных предикатов вычислялись
и выбрасывались.

### Итог шагов 1 и 1б

| | было | стало |
|---|---:|---:|
| ядро целиком | 253 с | **100 с** |
| вызовов `sympy.factor` | 3 491 878 | 0 вне символьного пути |
| `exact_sign` на рациональном | 36.2 мкс | **0.30 мкс** |
| `ExactScalar.from_value` | 4.85 мкс | **0.80 мкс** |
| дайджесты | — | не изменились |
| знак с радикалом | 3334 мкс | **~130 мкс** |
| тестов в ядре | 227 | 576 |

**Важно про профилирование.** Здесь стояла запись «`cProfile` завышает cumtime,
`exact_sign` на самом деле 0.30 мкс, профилю не верить». Она была **неверной**,
и из-за неё шаг 5 нашёлся на месяц позже, чем мог. Профиль был прав.

Обманул микробенчмарк: он вызывал `exact_sign` в цикле на **одном и том же**
значении, а SymPy кеширует вывод предположений на самом объекте. Второй вызов
и дальше — 0.24 мкс, первый — 39.6 мкс. В поле каждая точка пересечения
уникальна, поэтому «первый вызов» — это почти каждый вызов.

Правило: **мерить на свежих значениях.** Если бенчмарк переиспользует объект,
он меряет кеш, а не работу. Расхождение бенчмарка с профилем — повод не верить
бенчмарку тоже, а не только профилю.

### Шаг 2 — убрать общий сериализатор из конструирования числа ✅

Замер показал, что менять сам тип не требуется: `ExactScalar.from_value` стоил
4.85 мкс, из них 4.19 мкс — `sp.srepr`, то есть общий обход дерева выражения
ради строки вида `Rational(p, q)`. Чтение уже было дешёвым: `as_expr` попадает
в lru-кэш в 98.8% случаев.

Введены `_rational_srepr()` и якорный разбор в `_parse_expr()`: та же строка
собирается и разбирается напрямую. Строка остаётся идентичностью `ExactScalar`
и входом дайджестов, поэтому эквивалентность проверена побитово на 6020
значениях (включая алгебраические, которые обязаны идти прежним путём) и
закреплена в тестах, включая отдельный случай «составное выражение не должно
быть принято за рациональное по префиксу».

| | было | стало |
|---|---:|---:|
| `ExactScalar.from_value` | 4.85 мкс | **0.80 мкс** |
| `srepr` рационального | 4.18 мкс | **0.25 мкс** |

Замена `ExactScalar(str)` на `Fraction` / `ℚ(√d)` **не понадобилась** и снята
с плана: после этого шага конструирование стоит 0.80 мкс, и структурная
перестройка основного типа данных не окупает риска. Вернуться к ней стоит
только если профиль снова упрётся в конструирование.

### Шаг 3 — сертифицированный интервальный фильтр знака ✅

Замер сначала показал, куда именно текут радикалы: 45% времени прогона уходило
в символьный путь, причём **различных радикалов всего 5**, и 99.6% из них —
простые `√2`, `√3`, `√5` (приходят из повёрнутых плоскостей в фикстурах и из
нормализации в `unit()`).

Вместо того чтобы вычищать радикалы из предикатов (перестройка Gram-метрики,
дорого и рискованно), поставлен фильтр перед символьным путём: строгая
**интервальная оболочка** выражения. Каждая операция расширяет интервал наружу,
поэтому истинное значение гарантированно внутри. Если оболочка целиком по одну
сторону от нуля — знак **доказан**; иначе управление уходит в прежний точный
путь без изменений.

Порога здесь нет — есть сертификат. Тождественный ноль интервалом доказать
нельзя, и он корректно проваливается в точную арифметику; на это есть тест.

| | было | стало |
|---|---:|---:|
| ядро целиком | 126 с | **100 с** |
| знак с радикалом | 3334 мкс | **~130 мкс** |
| доля запросов, доказанных интервалом | — | **100%** (0 уходов в символьный путь) |
| дайджесты | — | не изменились |

Это тот же приём, которым Boost.Polygon добивается скорости: дешёвый фильтр
отвечает почти всегда, точная арифметика остаётся для спорных случаев.

**Что осталось символьным:** канонизация алгебраических значений
(`_canonical_expr`, 5816 вызовов) и `exact_normalize` (2018). Обе строят
каноническую строку, которая идёт в дайджест, поэтому трогать их без
переопределения канонической формы нельзя. Это отдельное решение, не
оптимизация.

### N0 — near-planar политика ✅ (вне очереди, разблокирована владельцем)

Найдено при проверке разрешения на эвристики: `planar_metric.py` требовал
`_dot3(p - o, n) != 0` — побитово точный ноль. Ядро отвергало любой патч,
вершины которого не компланарны в точности, то есть практически любой
повёрнутый или отредактированный меш. Это ограничивало ядро сильнее, чем
производительность.

`NEAR_PLANAR_PROJECTION_V1` запрашивается явно; проекция точная в дробях;
сертификат записывает метод, бюджет, масштаб и поимённо спроецированные
вершины. Выход за бюджет — именованный отказ, а не тихое сглаживание.

### БЛОКЕР — угловой сертификат требовал рациональной доли π ✅ снят по тестам

Полевой прогон вскрыл настоящий блокер, и это была не производительность и не
планарность. Центральный NGON отвергался с
`ENVELOPE_DEBUG_EXACT_ANGULAR_CERTIFICATE_UNAVAILABLE`
(`Receipts: Metric 13/13 | Raw 12/13 | Resolved 0/13`), потому что
`atan2(sin, cos)/π` обязана была быть точной дробью: 90° → 1/2, 45° → 1/4.
Валили не экзотические углы, а шум координат на прямых — 89.9996° даёт долю
0.4999980324. Требование точности к данным, которые по природе binary64.

Измерение угла ушло в ядро: `reference/angle_measure.py`. Точный путь остался
байт-в-байт (дайджест прямого угла закреплён тестом), но перестал быть
единственным. Где дробь не рациональна, `mpmath.iv` строит строгую оболочку
`atan2(|sin|, cos)/π` — округление наружу на каждой операции, ширина ~1e-28.
Порога здесь нет: оболочка либо доказывает `π < φ < 2π`, либо даёт именованный
отказ. Тот же приём, что и в шаге 3 для знака.

Побочно закрыт тупик точного пути: `Decimal.scaleb` округляет φ ∈ (1, 2) до
28 значащих цифр, поэтому φ и δ расходились и **точный** 60° угол отвергался
собственной сверкой. Теперь точный путь — фильтр: не удержал разряд, отвечает
оболочка. Рациональная арифметика при этом не тронута.

| | было | стало |
|---|---:|---:|
| требование к доле π | точная дробь | доказанная оболочка |
| `envelope_request_export.py` | 3107 строк | **3053** (храповик опущен) |
| дайджесты | — | не изменились |
| тестов в ядре | 584 | 626 |

Остаётся один способ не получить ответ по углу, и он именованный: интервал,
накрывающий границу полосы `δ ∈ (k/3, (k+1)/3]`, не доказывает уникальный k —
`ANGULAR_PROFILE_SELECTION_UNCERTAIN`. Диагностика:
`tools/blender_probe_corner_angles.py` показывает углы у границ полос.

**Приёмка владельцем не пройдена:** ожидается `Raw 13/13` (было 12/13) и
огибающая на центральном патче `building.002`. До этого прогона считать блокер
закрытым нельзя — урок Фазы 2 про «мерить в поле, а не по тестам» в силе.

### Шаг 5 — знак рационального не будит машину предположений SymPy ✅

Найдено при разборе вопроса владельца «точность конструкций против точности
предикатов». Проверялась гипотеза «раздувание коэффициентов»: разрядность
числителя и знаменателя на 1 305 764 реальных значениях —

| перцентиль | 50 | 90 | 99 | 100 |
|---|---:|---:|---:|---:|
| бит | 3 | 9 | 19 | **29** |

Ни одного значения шире 64 бит. **Раздувания нет**, отказ от точных конструкций
не выиграл бы ничего на этой оси: числа и так помещаются в машинное слово.

> **Поправка по полевому замеру (см. шаг 4).** Эти 29 бит — тестовый корпус. В
> поле `ARRANGEMENT_MAX_COORDINATE_CHARS` на центральном патче `building.002`
> равен 338 символам канонической записи, то есть примерно по 160 десятичных
> цифр на числитель и знаменатель — около 1060 бит, в 36 раз шире максимума
> корпуса. Вывод шага 5 при этом **остаётся верным, но по другой причине**:
> отдельный замер (та же геометрия, только длина координат) даёт 1.6× при
> вдвое большей длине, чем в поле. Длина чисел действительно не та ось —
> не потому, что числа короткие, а потому, что цена по ней растёт слабо.
> Правило то же: утверждение, полученное на фикстурах, в поле перепроверяется.

Настоящая причина оказалась в одной строке `exact_sign`:

```python
return 1 if expression.is_positive else -1   # 39.18 мкс на новом значении
return (expression.p > 0) - (expression.p < 0)   # 0.097 мкс
```

`is_positive` — не флаг, а **запрос к машине предположений**: на ещё не виденном
`Rational` SymPy выводит всю решётку фактов (`deduce_all_facts` — 885 625
вызовов, ~50% self-time профиля). Соседние `is_Rational`, `is_Integer`, `is_zero`
стоят по 0.04 мкс — это флаги класса, и они ни при чём. Знаменатель `Rational`
всегда положителен, поэтому знак берётся из числителя.

| | было | стало |
|---|---:|---:|
| `test_session_d_resolved_coverage.py` | 32.4 с | **10.5 с** (3.1×) |
| знак нового рационального | 39.6 мкс | **0.097 мкс** (400×) |
| `deduce_all_facts` в профиле | 885 625 вызовов, ~50% | **отсутствует** |
| дайджесты | — | не изменились |

Согласие проверено на 20 005 значениях включая краевые (`0`, `Rational(1,-2)`,
`Rational(-6,-4)`, 10³⁰). Регресс закрыт наблюдаемым следом: запрос
предположения дописывает факты в `_assumptions` объекта, и тест требует, чтобы
быстрый путь оставил кеш нетронутым.

После этого шага профиль **плоский**: ни одна функция не даёт больше ~10%.
Следующие резервы — линейные по размеру результата, не по геометрии:
`codec.to_canonical_data` (1.27M рекурсивных вызовов) и `_json_sort_key`, который
сериализует каждый элемент frozenset целиком только ради сортировки.

### Шаг 7 — очередь событий волнового фронта ✅ ядро (Q1) + motorcycle graph (Q2) + дифференциальная сверка (Q3)

`kernel/src/cftuv_envelope/wavefront/` — отдельный модуль, ни к чему не
подключён, дайджесты не тронуты.

**Сверка с попарным кроем сделана, и она ОТМЕНЯЕТ план «проводить как есть»
(Q3).** Срез ничего не переключал: он посчитал покрытие обоими путями на одном
входе и сравнил точно. Три результата, каждый с числом.

*Первое: вход `bf6` в очередь не отображается, и причин три независимых.*
Сопоставление законов прихода с рёбрами домена идёт в точных дробях, поэтому
каждая причина мерена отдельно, а не выведена из предыдущей.

> ЭТОТ СЧЁТ ЗАКРЫТ ПОЗЖЕ. Третья причина ушла на шаге 7в (стены), первые две — на
> шаге 7г (рациональное `q` и решётка карты). `bf6` отображается, скелет на нём
> считается за 42–55 мс. Числа ниже оставлены как измерение своего дня.

| причина | число |
|---|---|
| домен не на целой решётке | 9 вершин из 12 |
| скорости прихода не единичные | `(s/\|n\|)²` = 137438953472/844687660141 (два закона) и 17179869184/1439659412197 (третий) |
| источник — не вся граница | 3 ребра домена из 12 |

Вторая причина глубже двух других: разброс `(s/\|n\|)²` равен **13.6350**, то есть
евклидовы скорости фронтов различаются в **3.6926 раза**. На `bf6` нужен
**взвешенный** straight skeleton, а не единичный. Арифметика к нему готова (`q` —
свободное целое поле), геометрия нет: `SupportLineV1.through` строит только
единичную скорость, а теорема 2.11 опирается на `dist(p, line) = t`, что при
`s ≠ |n|` неверно.

*Второе: на `bf6` попарный крой теряет ВСЁ покрытие, и резать там было нечего.*
`RawCoverage` = 122766786560/373821260323, союз внутри Policy B = **0**. При этом
три юбки до кроя попарно не пересекаются — точная площадь каждого пересечения
равна нулю, а сумма их площадей (27023638528 + 27023638528 + 68719509504) совпадает
с `RawCoverage` целиком, — то есть верный ответ был «не резать ничего».
Ноль лежит на ВХОДЕ охранника, не на выходе конвейера:
`resolve_coverage_interactions` отдаёт `INTERACTION_POLICY_B_PARTITION_UNPROVEN` и
`resolved_coverage = None`, а пустой союз — то, что охранник получил и отверг.
Сохранили хоть один регион **0 из 9** вкладов.

*Третье, и это главное для проводки: два пути покрывают РАЗНЫЕ МНОЖЕСТВА.* Юбка
попарного пути — перпендикулярная полоса, обрезанная концами своего ребра; грань
скелета обрезана биссектрисами и выходит за концы ребра. У вогнутой вершины на 90°
полосы оставляют незакрытый квадрат со стороной α:

| фигура | α | RawCoverage | очередь | разность |
|---|---:|---:|---:|---:|
| `ell` | 1 | 43 | 44 | 1 |
| `ell` | 3/2 | 243/4 | 63 | 9/4 |
| `ell` | 2 | 76 | 80 | 4 |
| `ell` | 3 | 99 | 108 | 9 |

Ровно `α²`. Дыру оставляет попарный путь, а не очередь: модель владельца требует
«поле закрывается полностью, дыры быть не должно». **Но проверка `policy_b.py:976`
в нынешнем виде очередь не пропустит**, потому что её эталон — `RawCoverage`, то
есть множество полос вместе с этой дырой. Следующий срез обязан сменить эталон
проверки, иначе верный ответ будет отвергнут верной проверкой неверного эталона.

Ответов **три**, не два, и это меняет смысл расхождения. Проверено замкнутой
формой плюс сеткой 4000×4000, вне обеих реализаций. `ell` — квадрат 12×12 без
правого верхнего квадранта, площадь 108. При α = 1:

| множество | площадь | вогнутая вершина | на сетке |
|---|---:|---|---:|
| полосы (`RawCoverage`) | 43 | **не накрыта вовсе** | 42.962 |
| **митрованный** отступ = очередь | 44 | острый угол | 43.960 |
| **круглый** отступ (Минковский) | 43 + π/4 | дуга радиуса α | 43.747 |

Митрованный = `108 − |[α,12−α]×[α,6−α] ∪ [α,6−α]×[α,12−α]|` даёт 44 / 63 / 80 / 108 —
колонку **очереди**. Спор локализован до квадрата `[5,6]×[5,6]` у вершины `(6,6)`,
и **это не дыра, которую надо заткнуть, а ручка профиля вогнутого угла**: полосы
не накрывают его вовсе, митрованный — целиком (`α²`), круглый — на `π/4` от него,
`LINEAR_REFLEX_EQUAL_V1`/ROUND_k — между ними по числу звеньев. То есть `α²` —
верхняя граница семейства, а очередь реализует ровно одну его точку.

Полосы неверны при **любом** профиле. Очередь верна для митрованного и обязана
стать параметризованной по профилю. Осторожно с названием: «эрозия» — это
круглый отступ, третье число; исполнитель, реализовавший её, объявил бы дефектом
верный митрованный ответ.

*Где очередь проходит там, где крой падает.* Прямоугольный треугольник с катетом
12 при α = 1: `RawCoverage` = `21 + 10√2`, крой отказывает
`INTERACTION_POLICY_B_PARTITION_UNPROVEN`, очередь даёт **ровно** `21 + 10√2` —
тот же канонический набор коэффициентов. Отрицательный контроль пройден: на
выпуклом квадрате 8×8 при α = 1, 2, 4 совпадают все три величины.

Сравнивалась **точная площадь**, тем же предикатом `exact_sign`, которым крой
проверяет себя на `policy_b.py:971`. Дайджесты не сдвинулись: **46 из 46** (44
скелета корпуса на обоих путях поиска split плюс дайджест и площадь `RawCoverage`
полевой фикстуры), сверено с извлечённой копией дерева на коммите `5ab0b89`.

Побочно найдено стендом: `resolve_coverage_interactions` пропускает наружу
`ExactArrangementRotationSystemUnproven` вместо именованной диагностики — то есть
в поведении есть исход, которого нет в `InteractionOutcome`.

Воспроизведение:
`PYTHONPATH=kernel/src:kernel/tests:tools python3 tools/wavefront_differential.py`.

**Арифметика события решена, и это главный итог.** Время события иррационально —
степень 1, 2, 4 или 8 — но его не нужно вычислять. Ребро решётки даёт прямую
`a·x + b·y = c + t·√q` с целыми `a, b, c, q`; событие есть обращение в ноль
определителя трёх таких прямых, а он **аффинен по t**: `D0 + t·S`, где
`S = Σ C_i·√q_i`. Время хранится парой `(D0, S)`, и тогда
`sign(t₁ − t₂) = sign(d₁·S₂ − d₂·S₁)` — линейная комбинация, без деления и без
корня. Равенство чисто рационально: канонический вид по различным бесквадратным
радикандам единственен. **Очередь точная и беспороговая при иррациональных
временах** — этого нет ни у Kendzi (`ε = 1e-10`), ни у Трики (квантование 1 мм).

**Motorcycle graph закрыл наивный перебор (Q2).** Оба пути мерены рядом, в одном
прогоне, на одной машине, кеши `squarefree_split`/`prime_support` сброшены между
точками:

| вершин | кандидатов, перебор | кандидатов, граф | секунд, перебор | секунд, граф |
|---:|---:|---:|---:|---:|
| 16 | 84 | **35** | 0.0198 | 0.0267 |
| 128 | 7 812 | **399** | 0.2702 | 0.2436 |
| 894 | 396 940 | **2 887** | 5.6639 | 1.8054 |
| **2002** (999 reflex) | **1 998 000** | **6 488** | 24.06 | **4.10** |

| показатель по секундам | перебор | граф |
|---|---|---|
| 16→128 | O(n^1.26) | O(n^1.06) |
| 128→894 | O(n^1.57) | O(n^1.03) |
| 894→2002 | O(n^1.79) | O(n^1.02) |
| 16→2002 целиком | O(n^1.47) | **O(n^1.04)** |

> **ЭТОТ ПОКАЗАТЕЛЬ ОТНОСИТСЯ К ГРЕБЁНКЕ, А НЕ К ЛЮБОМУ КОНТУРУ.** Замер снят
> `tools/benchmark_wavefront_scaling.py`, а он строит `comb(teeth)` — фигуру, у
> которой вогнутые вершины стоят В РЯД, трассы коротки и не пересекаются. Это
> лучший случай для индекса по ячейкам, а не типичный. Перемерено 2026-07-27
> одним и тем же кодом на двух семействах:
>
> | семейство | 128–256 вершин | мкс на вершину | показатель |
> |---|---:|---:|---|
> | гребёнка | 0.27–0.62 с | ~2 100–2 400 | O(n^1.05) |
> | звезда (вогнутые в общем положении) | 21 с при 129 | ~163 000 | O(n^1.96–2.1) |
>
> Разница на вершину — **77×**, и это форма, а не размер. Показатель O(n^1.04)
> верен и не отзывается, но он НЕ общий, и подавать его как характеристику
> алгоритма нельзя: на контуре общего положения очередь идёт как O(n²).
>
> Что это значит для продукта, решается размером домена, а не спором про
> алгоритм. Единственная полевая фикстура даёт **12 вершин** на домен, и там
> цена 0.02–0.07 с при любой форме — против 1.85 с на домен у нынешнего пути
> (13 доменов за 24 с). Но фикстура одна, а размеров остальных доменов никто не
> мерил. Меряет `tools/blender_probe_domain_sizes.py`, и до этого замера
> утверждать «очередь успевает в поле» нельзя.
>
> Это третья подряд запись, где закон оказался снят на нетипичном входе
> (первые две — область закона `-(w-t)²` и знаменатель корпуса). Корень тот же:
> **вход замера выбирался под удобство замера, а не под то, что бывает в поле.**

Итог при 2002 вершинах: **кандидатов в 308 раз меньше** (6 488 против 1 998 000),
время в 5.9 раза меньше, показатель степени перестал расти на хвосте.
**`sign_by_conjugation = 0`** на обоих путях — дорогой решатель знака не входил ни
разу и после появления произведений `SqrtSum`. Цена, которую граф берёт себе,
названа: `sign_queries` 79 499 → 94 217 (+18%), плюс 4 995 проверок стен и 999
шагов марша.

Для сравнения: `mesh_inset` Трики на **1024** вершинах — **100.9 с** на float с
допусками, показатель O(n^2.30).

**Что именно дала теорема 2.11.** Reflex-вершина волны не уходит за свою трассу,
а расстояние от точки split до несущей прямой рассекаемого ребра РАВНО времени
события (нормаль в `SupportLineV1` имеет длину ровно `√q`). Значит прямая
кандидата проходит не дальше времени крушения от трассы — это пространственный
запрос, и он решается сеткой ячеек вместо перебора рёбер. Проверка попадания в
текущий отрезок фронта осталась на месте: она необходима, но не достаточна.

**Дайджесты не сдвинулись ни один: 26 из 26** (8 в корпусе теста, 18 в корпусе
стенда), сверено с извлечённой копией пакета на коммите `1586f2d`. Полный перебор
сохранён первым классом (`SplitSearch.EXHAUSTIVE`) как эталон дифференциального
теста: split-события совпадают узел в узел на корпусе и на 100 контурах общего
положения.

**Остался шов `EXTENDED_WAVEFRONT_NOT_BUILT_IN_THIS_SLICE`.** `START` и `SWITCH`
рождаются не из графа, а из расширенного фронта: Steiner-вершины графа обязаны
стать вершинами самого фронта, а фронт — вестись по ячейкам арранжемента трасс.
Граф Steiner-вершины даёт и считает (`resting_steiner_vertices = 2` на фикстуре
`star_9_seed_4`, где мотоциклы гасят друг друга), фронт по ячейкам не ведётся,
поэтому счётчики `start_events`/`switch_events` равны нулю с названной причиной.

### Шаг 7в — частичный источник: неподвижная часть границы ✅ стены и веса одним обобщением

Срез закрыл ПЕРВЫЙ из двух блокеров полевого запуска и наполовину второй, потому
что оба оказались одним обобщением.

**У ребра появилось своё `q`.** `PolygonV1` больше не считает источником всю
границу: `q` — квадрат скорости в единицах нормали, ноль означает СТЕНУ (прямая
не двигается), `|d|^2` — источник единичной скорости (умолчание), что угодно ещё
— взвешенное ребро. Флаг «стена» не заводился намеренно: стена и вес — один и
тот же выход за тождество `q = a^2 + b^2`.

**Единичная скорость снята там, где была зашита.** Тождество `a^2 + b^2 == q`
держалось на всех 173 рёбрах корпуса ПО ПОСТРОЕНИЮ `SupportLineV1.through`,
поэтому найдено оно чтением, а не падением:

| место | роль `q` | правка |
|---|---|---|
| `sliding_point`, `scale = 1/q` | длина нормали | `normal_squared`; при `q = 0` было делением на ноль |
| `sliding_time`, два вхождения | длина нормали | `normal_squared` |
| теорема 2.11 (`motorcycle.py`) | скорость | обобщена множителем `speed_bound_of` |
| шесть прочих мест чтения | скорость либо тождество прямой | верны при любом `q`, не тронуты |

**Цена обобщения теоремы 2.11 равна нулю, и это замер.** `split_candidates_examined`
совпали с прежними ПОБИТОВО на всех 22 фигурах корпуса и обоих путях поиска —
402 у motorcycle, 457 у полного перебора. Альтернатива (отказ от индекса на
неединичных рёбрах) стоила бы возврата к `reflex * рёбра`: 1 998 000 кандидатов
против 6 488 при 2002 вершинах.

**Ответ проверен НЕЗАВИСИМО, и эталон для этого расширен.**
`mitered_standard.partial_source_standard` — объединение митрованных полос
рёбер-источников (шары Чебышёва вокруг ОТРЕЗКОВ, коридор видимости), целиком в
дробях. Корпус частичного источника — 41 фигура, полный перебор подмножеств
«одно ребро» и «пара соседних» на прямоугольнике, `ell` и лестнице плюс три
взвешенные записи:

| доля корпуса | фигур | что означает |
|---|---:|---|
| очередь сверена с эталоном точно | **22** | 176 строк из 176 при восьми alpha |
| очередь отказывает по имени | **18** | источник не заметает фигуру: конец цепочки едет по продолжению прямой стены |
| эталон отказывает по имени | **1** | разные скорости в вогнутой вершине: митрованный угол там не квадратный |

Отрицательный контроль: у фигуры, где источниками помечены ВСЕ рёбра,
расширенный эталон даёт прежние числа — `ell` **44 / 63 / 80 / 108** при
alpha = 1, 3/2, 2, 3, и совпадение с прежним эталоном на всех 54 строках
корпуса, где тот применим. Формулы две и разные.

**Полевой патч: находок стало ДВЕ из трёх.**

| находка на `bf6` | было | стало |
|---|---|---|
| `DOMAIN_IS_NOT_ON_THE_INTEGER_LATTICE` | 9 вершин из 12 | **9 вершин из 12** |
| `ARRIVAL_LAW_IS_NOT_UNIT_SPEED` | спред 13.634951522381032 | **спред 13.634951522381032** |
| `SOURCE_IS_NOT_THE_WHOLE_BOUNDARY` | 3 ребра из 12 | **ушла**: 3 источника и 9 стен, разметка однозначна |

**Что осталось от взвешенного скелета — три места, и все про отображение закона
в `q`, а не про геометрию.** Геометрия работает: `ell` и лестница с ребром вдвое
быстрее считаются до конца и совпадают с эталоном при `speed_bound = 2`.

1. `q` обязано быть целым (под корнем у `SqrtSumV1` целое). На `bf6` это требует
   рёбер длиной порядка 919 068 и 1 199 858 узлов решётки — при масштабах
   привязки 64 / 256 / 1024 таких нет.
2. Общий пересчёт масштаба (умножить все `q` на `M = 1216062540288651366139777`)
   скелет не меняет, но делит времена на `sqrt(M)`, а `coverage_at` принимает
   alpha `Fraction`. Упор в ТИП alpha, не в геометрию.
3. Мост в эту сторону не ходит: `ARRIVAL_LAW_IS_NOT_UNIT_SPEED` остаётся
   находкой. Его настоящее имя — «скорость не представима целым `q`».

Ворота: ядро **1552** (было 1411), хост **858 passed, 3 skipped**, архитектура
**327**. Под `kernel/src` тронут только `wavefront/`.

### Шаг 7д — контур грани обязан быть простым ✅ третья граница; порядок сборки починен, измерен и НЕ поставлен

Срез закрыл класс отказа, а не одну фигуру: **перекрученный контур больше не
проходит молча.** У `build_faces` было две объявленные границы, и обе читают
ПЛОЩАДЬ, а площадь берётся формулой трапеций ПО КОНТУРУ. Перекрути контур — та
же формула посчитает кусок дважды либо с обратным знаком, и обе границы смолчат.

**Третья граница поставлена:** контур каждой грани обязан быть ПРОСТЫМ, то есть
никакие два его сегмента не пересекаются трансверсально. Проверка точная
(`SqrtSumV1.sign()` на ориентациях, ни одного порога), в `detail` лежат число
пересечений и ПАРЫ СЕГМЕНТОВ. Порядок опроса — простота, положительность,
сумма, — от причины к следствию.

| что измерено | число |
|---|---:|
| `bf6`: самопересечений на грани из трёх | **3**, пары `(5,9) (6,9) (7,9)` |
| `bf6`: избыток суммы при этом | +2.90 % (112 055 108 527 против 108 901 947 644) |
| граней корпуса `named_corpus()` с пересечением | **0 из 185** |
| строк сетки крестов с `FACE_IS_NOT_POSITIVE`, у которых ЕСТЬ пересечение | **32 из 32** |
| строк сетки крестов с `EXACT`, у которых есть пересечение | **0 из 211** |

Третья строка снизу — находка вне карточки среза: **остаток на кресте при
`|w - t| >= 2*arm` — та же болезнь, что на `bf6`.** Разошлись только знаки: на
кресте перекрут делает грань отрицательной, на `bf6` оставляет положительной и
раздувает сумму. Граница трансверсальности выбрана замером, а не вкусом: более
строгая («никаких общих точек») отвергает пять ВЕРНЫХ фигур корпуса частичного
источника, где два узла скелета стоят в одной точке.

**Порядок сборки починен, измерен целиком и НЕ поставлен.** Кандидат — правило
СМЕЖНОСТИ вместо монотонности: две точки дальней границы грани соседние тогда и
только тогда, когда у них есть общий второй участник. Порядок при этом чисто
комбинаторный — ни одного знака `SqrtSumV1`.

| проверка кандидата | число |
|---|---:|
| граней корпуса, где совпал с поставленным | **185 из 185** |
| граней сетки крестов, где совпал | **2884 из 2916** |
| строк креста, которые чинит начисто | **32 из 32** |
| `bf6`: сумма граней против площади домена | **108 901 947 644 из 108 901 947 644** |
| `bf6`: покрытие при alpha запроса (0.25, то есть 16384 в решётке 65536) | ~3 018 411 398 удвоенных, **2.7717 %** домена |
| фигур частичного источника, где ПРОТИВОРЕЧИТ эталону | **3** |

Три противоречия и есть причина не ставить: на `ell_12_source_edge_4` кандидат
даёт 120 против 96 у эталона при alpha 8. Корень измерен и он НЕ в сборщике:
`skeleton.py` гасит стену `(12,6) -> (6,6)` целиком в момент, когда фронт лишь
касается её единственной точкой, и фронт скачком расширяется с ширины 6 до 12.
Прежний неверный порядок это скрывал случайно — давал недостаток площади, и
громкий отказ выглядел верным ответом, будучи верным по неверной причине.

**Порядок работ отсюда следует, а не выбирается:** сперва коллинеарная
неподвижная стена в `skeleton.py`, потом замена правила сборки. Кандидат
сохранён исполняемым (`kernel/tests/adjacency_chain.py` и четыре теста), чтобы
следующий срез начинал с измеренного, а не с пересказа.

Ворота: ядро **1594** (было 1565), хост **861 passed, 3 skipped**, архитектура
**330**. `FROZEN_DIGESTS` не двинулись, сверка `ell` с замкнутой формулой на
месте — 44 / 63 / 80 / 108. Под `kernel/src` тронут только `wavefront/`.

### Шаг 7е — коллинеарная неподвижная стена ✅ фронт не гасит того, чего не касается

Срез снял ЕДИНСТВЕННОЕ препятствие к постановке правила смежности, и снял его
там, где лежал корень, — в `skeleton.py`, а не в сборщике граней.

**Болезнь.** `concurrency_time` отвечает про три ПРЯМЫЕ, а гаснет ОТРЕЗОК. Когда
движущаяся прямая источника наезжает на прямую коллинеарной стены, тройка
сходится честно, хотя отрезки перекрываются в единственной точке и стена не
сжимается ни на шаг. Числа трёх фигур корпуса, замороженные поточечно:

| фигура | стена | источник | t | фиктивный узел | отрезок стены | отрезок источника |
|---|---|---|---:|---|---|---|
| `ell_12_source_edge_4` | `(12,6)->(6,6)` | `(6,12)->(0,12)` | 6 | `(12,6)` | `[-72, -36]` | `[-36, 0]` |
| `staircase_source_edge_6` | `(8,8)->(4,8)` | `(4,12)->(0,12)` | 4 | `(8,8)` | `[-32, -16]` | `[-16, 0]` |
| `staircase_source_edges_4_5` | `(12,4)->(8,4)` | `(8,8)->(4,8)` | 4 | `(12,4)` | `[-48, -32]` | `[-32, 0]` |

**Правило поставлено БЕЗ частного случая.** «Перекрытие по мере» им быть не
может: у соседей по фронту перекрытие равно нулю всегда, и такое правило пришлось
бы обусловить коллинеарностью. Поставлено сильнее и общее — **схлопываемый
отрезок обязан сжаться в ТОЧКУ**, точным равенством двух проекций
(`SqrtSumV1.is_zero`). Отказ назван `FILTER_SPAN_DOES_NOT_COLLAPSE`. У `_span_end`
появился второй ответ для НЕПОДВИЖНОГО ребра: его прямая не двигается, значит
отрезок фронта — подотрезок самого ребра при любом `t`.

| контроль | число |
|---|---|
| `named_corpus` (22 фигуры) | исходы, узлы и счётчики совпали ПОБИТОВО |
| `named_corpus`: срабатываний нового отказа | **0** — стен там нет |
| `FROZEN_DIGESTS` | не двинулись |
| `ell` полный источник | 44 / 63 / 80 / 108 |
| частичный источник: расклад 22 / 18 / 1 | не изменился |
| частичный источник: изменились фигуры | **3 из 41**, все в сторону громкого отказа |
| `bf6` | `maps == True`, 3 источника / 9 стен, скелет `EXACT` 10 узлов, новый отказ **0 раз** |

**Препятствие кандидату сборки снято числом:** противоречащих эталону фигур стало
**0 вместо 3**, а фигур, где кандидат превращает отказ в `EXACT`, — **1 вместо 4**
(`staircase_source_edges_7_0`, совпадает с эталоном на всех восьми alpha).
Постановка кандидата — следующий срез.

**Противоречие с карточкой среза, названное числом.** Требование «покрытие обязано
совпасть с эталоном» на этих трёх фигурах недостижимо в принципе: эталон при
alpha = 10^6 даёт 144 из 216, 96 из 192 и 160 из 192 удвоенных, то есть источник
до части фигуры не доходит НИКОГДА, а граница 1 сборщика требует воспроизведения
всей площади. Достижимый критерий — громкий именованный отказ вместо тихого
неверного числа, и он выполнен.

**Открытый счёт, НЕ закрытый.** Верное продолжение фронта известно: у
`ell_12_source_edge_4` при `t > 6` граница незаметённой области содержит отрезок
на прямой `x = 6` вниз от `(6,6)`, то есть прямую погашенного ребра с ОБРАТНОЙ
нормалью — схлопнувшееся ребро обязано развернуться, а не умереть. Правила нет,
конфигурация названа `NO_RULE_JOINT_IS_CODIRECTIONAL_AT_DIFFERENT_SPEEDS`.

Ворота: ядро **1676** (было 1594), хост **861 passed, 3 skipped**, архитектура
**330**. Под `kernel/src` тронут только `wavefront/`.

### Шаг 7г — отображение полевого входа ✅ `bf6` отображается, очередь считается на настоящей геометрии

Срез закрыл ВТОРОЙ блокер полевого запуска: последние две находки `bf6` ушли, и
вход попал в очередь целиком.

**Маршрут выбран замером, и маршрут «поднять масштаб» оказался мёртв.** Условие
целого `q` — это `B | |d|^2`, где `B` — знаменатель `(s/|n|)^2`. Оба знаменателя
`bf6` БЕСКВАДРАТНЫ (`844687660141 = 13·257·3413·74077`,
`1439659412197 = 11701·123037297`), поэтому минимальная длина ребра равна `B`
ЦЕЛИКОМ, а не `sqrt(B)`. Прежняя запись требовала 919 068 и 1 199 858 узлов —
это `sqrt(B)`, и она занижала требование в 919 069 и 1 199 858 раз.

| | требуется | доступно на самом мелком допустимом шаге | недостаток |
|---|---:|---:|---:|
| закон 1 и 2 | 844 687 660 141 | 942 195 | ×896 510 |
| закон 3 | 1 439 659 412 197 | 942 195 | ×1 527 984 |

Окно `bf6`: габарит патча 11 м, шаг `[1.54e-04, 1.00e-02]`, масштабы источника
128…4096, решётка карты (через `chart_grid_for`) 2048…65 536.

**`q` стало РАЦИОНАЛЬНЫМ, и стоило это одного места.** Тождество
`sqrt(p/r) = (1/r)*sqrt(p*r)` внесено в `SqrtSumV1.radical`. Мест чтения `q` —
18 строк; ломались о дробь ровно те 10, где `q` шло радикандом (13 аргументов),
и все чинятся этой одной правкой. На целом радиканде ветка не выполняется, поэтому
дайджесты и счётчики кандидатов корпуса (**402** motorcycle / **457** перебор на
22 фигурах) не сдвинулись ни на бит.

Цена факторизации измерена, а не оценена: радиканды порядка **1e34**, на `bf6`
различных **22**, `squarefree_split` — 1.66 мс на штуку холодным, 0.2 мкс из кеша.

**Пункт 2 прежнего списка отменился сам.** «Взвешенность упирается в тип alpha»
относилась к домножению всех `q` на `M = 1216062540288651366139777`; при
рациональном `q` домножения нет, и `t` совпадает с alpha закона точно, потому что
`q = (lambda*s)^2` и есть условие совпадения времён.

**Привязка домена не сохраняет классы несущих прямых: 0 из 12** на всех
проверенных масштабах. Поэтому сопоставление закона с ребром идёт по
ДОРЕШЁТОЧНЫМ дробям, а разметка переносится на привязанный полигон по ПОЛОЖЕНИЮ
ребра в петле. Запрет `REFERENCE_ARRANGEMENT_ROTATION_SYSTEM_UNPROVEN` на очередь
не распространяется: он про арранжировку `reference/`, а очередь строит и
валидирует свой полигон сама.

| находка на `bf6` | было | стало |
|---|---|---|
| `DOMAIN_IS_NOT_ON_THE_INTEGER_LATTICE` | 9 вершин из 12 | **ушла**: привязка, невязка 7.34e-06 ед. карты |
| `ARRIVAL_LAW_IS_NOT_UNIT_SPEED` | спред 13.634951522381032 | **ушла**: `q` рациональное у 3 рёбер |

**Первое измерение очереди на полевой геометрии.** Скелет `EXACT`, 10 узлов
(6 EDGE + 4 SPLIT), оба пути поиска — один дайджест и по 83 кандидата.
**42–55 мс** на построение, устойчиво по масштабу решётки; мост 0.8 мс, грани
1.5 мс. Сопряжение знака — **0 раз** при 439–549 решениях знака: интервальная
оболочка закрыла всё, и радиканды 1e34 узким местом не стали.

Отрицательный контроль: те же три источника с ЕДИНИЧНОЙ скоростью дают
`WAVEFRONT_LEFT_UNRESOLVED`. Взвешенность покупает сам ответ, а не точность.

**Что осталось и что изменилось в плане.** Грани на `bf6` отказывают, поэтому
покрытия у очереди на этом входе НЕТ — `coverage_at` отвечает
`PARTITION_IS_NOT_EXACT`, а не нулём. Новое здесь ЗНАК: сумма граней ПРЕВОСХОДИТ
полигон на **2.90%**, а в корпусе частичного источника все 4 случая этого исхода
были недостатком (избытков 0 из 4). Обе записанные причины сумму только
уменьшают, значит счёт причин неполон.

> Шаг 7д разобрал это до конца: причина ОДНА на все случаи — порядок сборки, — а
> знаки разошлись потому, что формула трапеций на перекрученном контуре даёт что
> угодно. Исход при этом переименован в свой корень: `FACE_CONTOUR_IS_NOT_SIMPLE`
> вместо `FACE_AREA_DOES_NOT_REPRODUCE_POLYGON`, и избыток стал 2.90 % по
> точному счёту, а не 2.95 %.

Ворота: ядро **1565** (было 1552), хост **860 passed, 3 skipped**, архитектура
**329**. Прирост хоста и архитектуры на 2 — от файла `tools/blender_probe_domain_sizes.py`
соседнего среза, а не от этого. Под `kernel/src` тронут только `wavefront/`.

### Шаг 7б — смена эталона покрытия ✅ независимый эталон (S1) + развязка закона сохранения (S2)

Срез закрыл второй из трёх вопросов предыдущей записи — «верно ли само покрываемое
множество», — тот, который не задавался НИКОГДА.

**Эталон посчитан вне обоих путей и целиком в дробях.** `kernel/tests/mitered_standard.py`:
митрованный отступ прямоугольной фигуры есть объединение вписанных осевых
прямоугольников, сжатых на α (`p + [−α,α]²` лежит в `P` ⟺ `p` лежит в каком-то
вписанном прямоугольнике, сжатом на α). Ни радикала, ни float, ни строки из
`wavefront/`. Точка семейства названа ЯВНО: митрованная, не круглая. Круглый
отступ («эрозия», Минковский по кругу) даёт третье число `43 + π/4`, и приняв его
за эталон, пришлось бы объявить дефектом верный ответ 44.

**Очередь совпала с эталоном ТОЧНО: 29 строк из 29, где сборщик граней отвечает**
(32 строки таблицы минус три `holes_2`, где он отказывает).

| фигура | вогн. | α | эталон | очередь | полосы | разность | квадраты | бюджет |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `axis_square` | 0 | 1 / 2 / 4 / 5 | 28 / 48 / 64 / 64 | то же | то же | 0 | 0 | 0 |
| `axis_rectangle` | 0 | 1 / 2 / 4 / 5 | 52 / 96 / 160 / 160 | то же | то же | 0 | 0 | 0 |
| `ell` | 1 | 1 / 3⁄2 / 2 / 3 | 44 / 63 / 80 / 108 | то же | 43 / 243⁄4 / 76 / 99 | α² | α² | α² |
| `hole_1` | 4 | 1 / 2 / 3 | 112 / 224 / 336 | то же | 108 / 208 / 300 | 4α² | 4α² | 4α² |
| `hole_1` | 4 | 6 | 336 | 336 | 336 | **0** | 144 | 144 |
| `holes_2` | 8 | 1 / 2 / 3 | 188 / 384 / 588 | **ОТКАЗ** | 180 / 352 / 516 | 8α² | 8α² | 8α² |
| `staircase` | 2 | 1 / 2 | 44 / 80 | то же | 42 / 72 | 2α² | 2α² | 2α² |
| `staircase` | 2 | 4 / 5 | 96 / 96 | то же | 96 / 96 | **0** | 32 / **39** | 32 / 50 |
| `u_shape` | 2 | 1 / 2 | 60 / 112 | то же | 58 / 104 | 2α² | 2α² | 2α² |
| `u_shape` | 2 | 3 / 5 | 135 / 135 | то же | 127 / 135 | **8** / **0** | 18 / 50 | 18 / 50 |
| `double_notch` | 4 | 1 | 76 | 76 | 72 | 4 | 4 | 4 |
| `double_notch` | 4 | 3⁄2 / 2 | 106 / 129 | то же | 197⁄2 / 119 | **15⁄2** / **10** | 9 / 16 | 9 / 16 |
| `double_notch` | 4 | 3 / 4 | 151 / 151 | то же | 145 / 151 | **6** / **0** | **33** / **44** | 36 / 64 |

Строк с вогнутыми вершинами **24 из 32**. Разность равна сумме `α²` на **15** из них
и меньше неё на **9**. «Меньше» разложено на две РАЗНЫЕ причины, а не свалено в одну
(на всех 9 сработала вторая, на 3 из них ещё и первая):

- **квадраты соседних вершин пересеклись** (`квадраты < бюджет`): `staircase`
  α = 5 → 39 против 50, `double_notch` α = 3 → 33 против 36, α = 4 → 44 против 64.
  Чище всего на кресте `4 × 4`: при α = 3 четыре квадрата объединяются ровно в
  площадь центрального блока — **16 против бюджета 36**, недобор 20 чистого
  пересечения, потому что разность там равна объединению;
- **до квадрата дошла ЧУЖАЯ юбка** (`разность < квадраты`): `hole_1` α = 6
  теряет все 144, `u_shape` α = 3 — 10 из 18, `double_notch` α = 3 — 27 из 33.

Вывод для проводки: **`α²` на вершину — верхняя граница, и она достигается только
пока квадраты порознь и чужие юбки не дошли.** Модель полос сверена с настоящим
`RawCoverage` конвейера на 12 входах (28, 48, 52, 96, 43, 76, 42, 72, 58, 104,
72, 119) — совпадение точное, поэтому разность считается не с выдумкой.

**Находка эталона, и она ОТМЕНИЛА доверие к `FaceOutcome.EXACT`.** На кресте
сборщик граней ломается двумя способами, и один был ТИХИМ:

| крест | что делает очередь | число |
|---|---|---|
| блок `4 × 4` | `WAVEFRONT_LEFT_UNRESOLVED` | граней нет, отказ назван |
| блок `5 × 4` | `FACE_AREA_DOES_NOT_REPRODUCE_POLYGON` | удвоенный дефект −1 |
| блок `6 × 4` | `FACE_AREA_DOES_NOT_REPRODUCE_POLYGON` | удвоенный дефект −4 |

Тихого больше нет: `build_faces` зовёт ОБЕ свои объявленные границы перед тем,
как вернуть `EXACT`, у каждой границы свой член исхода и ЧИСЛО потери в
`detail`. Цепочка вниз закрыта доказанно, а не чтением кода: `coverage_at` на
неточном разбиении отказывает `PARTITION_IS_NOT_EXACT` и ничего не считает —
раньше она честно выдавала 166 вместо эталонных 168.

**Причина найдена, и она НЕ та, что была записана.** Гипотеза «до семи узлов
грани имеют одинаковую проекцию, ломается порядок» опровергнута измерением на
всех 42 строках сетки: равные проекции есть (26 пар на фигуру), но все они
между записями узлов В ОДНОЙ ТОЧКЕ — из 18 узлов креста 10 повторы по точке.
Пар различных точек с равной проекцией НОЛЬ, порядок совпадающих точек на
площадь не влияет. Настоящая причина: у креста два гребня — горизонтальный и
вертикальный, — они пересекаются в центре блока, в точке
`(4 + wide/2, 4 + tall/2)`, там сходятся грани четырёх коллинеарных стенок, и
этой точки среди узлов скелета НЕТ ни на одной из 42 строк. Добавь её
кандидатом к четырём стенкам — дефект обнуляется точно на 36 строках, правка
ложится ровно на эти четыре грани и поровну. Сборщику нечем собрать правильную
грань: вершины ему не дали. **Корень в `skeleton.py`, а не в `faces.py`.**

**Закон дефекта имеет ОБЛАСТЬ, и прежняя запись «сошлось на всех 42» неверна:
сошлось на 36.** `удвоенный дефект = -(wide − tall)²` верен ровно пока
`|wide − tall|` не больше толщины рукава (у `cross` она 4). Шесть строк снаружи
области — (3,8), (3,9), (4,9), (8,3), (9,3), (9,4) — дают −20 и −24 вместо −25 и
−36, и это не длина рукавов: те же шесть расходятся при `right, top` = 40, 44 и
60, 64. Перебор по толщинам 2, 4, 6 на сетке 3..11 (по 72 строки) даёт предикат
без исключений. Диагональ `wide = tall` (7 строк) отказывает
`WAVEFRONT_LEFT_UNRESOLVED` — отказ и потеря сидят на одной фигуре по разные
стороны от `w = t`. Критерий готовности починки прежний и неподгоняемый: дефект
обязан обнулиться на всех 42 строках.

Неверная запись появилась потому, что закон мерился **не на фикстуре**, а на
кресте из одноразового скрипта с рукавом 6 вместо 4. При рукаве 6 граница
области лежит ровно за краем сетки 3..9 (максимальное `|w − t|` там как раз 6),
и потому не видна. Перепроверено обеими толщинами в одном прогоне: 4 → держится
36, 6 → держится 42. Правило того же класса, что «мерить на свежих значениях»:
**проверочный скрипт берёт фигуру из корпуса, а не воспроизводит её по памяти** —
своя копия входа отличается ровно теми параметрами, о которых не думаешь, и
именно они задают границу измеряемого явления.

**Ни одна прежняя защита это поймать не могла — все они односторонние.**
Граница 2 держалась (все 12 граней строго положительны), `coverage_at` тоже
отдавала `EXACT`, и её `does_not_exceed_the_polygon` проходила, потому что
потеря делает покрытие **меньше**, а не больше. Ловит только двусторонняя
проверка на равенство, и теперь её зовёт сама `build_faces`.

**СДЕЛАНО 2026-07-27, и корень оказался ТРЕТЬИМ.** Обе записанные причины
опровергнуты измерением: не порядок сортировки (опровергнут раньше) и не «слитая
вершина между сонаправленными стенками молчит» (ведущая гипотеза среза). Замер
365/21 антипараллельных при нуле сонаправленных был верен, и толковался он
неверно: сонаправленная вершина не создавалась ВОВСЕ — вместо неё стояли ДВЕ
вершины с отрезком нулевой длины между ними, поэтому о её позиции никто и не
спрашивал. «Не создаётся» и «создаётся и молчит» — разные болезни.

Настоящий корень: **вогнутая вершина, пришедшая не в РЕБРО, а в другую ВЕРШИНУ
фронта, разбиралась как разрез**, а разрез в конец отрезка рождает
отрезок-близнец нулевой длины. Теперь это встреча вершин с пересоединением по
лучам; вершина с развёрнутым углом получила позицию (скольжение по общей прямой
с сохраняющейся проекцией вдоль неё) и собственные времена событий.

| крест `6 × 4` | было | стало |
|---|---:|---:|
| узлов скелета | 18 | 7 |
| узлов на гребне `x = 7` | 3, 5, 5, 7, 7, 17 | 3, 6, 17 |
| пересечение гребней `(7, 6)` | нет | есть, участников 4 |
| дефект площади | −4 | 0 |

**Критерий готовности перевыполнен: 49 из 49 строк сетки 3..9, а не 42.**
Диагональ `wide = tall` больше не отказывает: в её единственной вырожденной
точке встречаются все четыре вогнутые вершины, и сшивка по лучам разбирает это
четырьмя аннигилирующими парами. Прежний закон дефекта `-(wide − tall)²` при
этом ОПРОВЕРГНУТ как объяснение: вынь пересечение гребней из верного скелета — и
закон не воспроизведётся ни на одной строке (−28 вместо −4 на `6 × 4`). Значит
прежний скелет отличался от верного тремя узлами, а не одним, и две ошибки
гасили друг друга в сумме площадей.

Остаток назван числом и границей: при `|wide − tall| >= 2 · arm` отказывает
`FACE_IS_NOT_POSITIVE` — это ДРУГАЯ вырожденная конфигурация (рукав схлопывается
раньше, чем до него доезжает скользящая вершина). Сетка 3..11 по трём толщинам:
было 0 из 81 на каждой, стало 51 / 70 / 64 при 0 / 9 / 17 строках известной
болезни общей несущей прямой.

**Тождество коллинеарных рёбер: обе болезни вылечены ✅ (2026-07-27).** Их было
ДВЕ, и объединять их было бы ошибкой — разные места, разные корни, разная цена.

*A — ключ участника.* Ключом служила несущая прямая `(a, b, c, q)`. В неё входит
`q = |d|²`, то есть ДЛИНА ребра, поэтому сталкивались только коллинеарные
сонаправленные рёбра ОДНОЙ длины (`holes_2`: 12 рёбер, 10 ключей; у креста все
12 ключей различны). Ответ на вопрос «прямая или вхождение» оказался «одна
сущность исполняла две роли»: геометрия по-прежнему идёт через `SupportLineV1`,
а тождеством участника стало вхождение ребра `(x0, y0, x1, y1)`. Роли разведены
на `_Edge.key` и `_Edge.line_key`; близнец разреза наследует вхождение.
`build_faces` даёт `EXACT` на **22 из 22** фигур корпуса (было 20 из 21), сетка
крестов по трём толщинам — **51 / 79 / 81** без пропущенных строк, у `holes_2`
площадь сходится 1344 из 1344 и покрытие совпадает с независимым митрованным
эталоном 188 / 384 / 588.

*B — сопоставление законов рёбрам.* Мост искал ребро для закона `next(...)` по
первой пропорциональной прямой, поэтому два коллинеарных ребра давали один
индекс, и `SOURCE_IS_NOT_THE_WHOLE_BOUNDARY` ЛГАЛ: на `cross(6,4)` и `holes_2`
источником было каждое из 12 рёбер, а мост говорил «частичный». Сопоставление
стало биекцией по классам прямых. Различать коллинеарные рёбра протяжённостью
НЕЛЬЗЯ, и это измерено: `PlainArrivalLawV1` протяжённости не несёт вовсе, значит
закон задаёт класс прямой, а счёт — размер наибольшего паросочетания (сложение
`min` по классам, граф есть объединение полных двудольных блоков). Владелец
внутри класса из нескольких рёбер назван неопределённым, а не выбран наугад.

Отрицательный контроль пройден: `bf6` по-прежнему отказывает всеми тремя
находками и с теми же числами (9 вершин из 12 вне решётки, спред скоростей
13.6349…, **3 сопоставленных ребра из 12**).

`cross` внесён в `named_corpus()` (блок `6 × 4`), корпус вырос с 21 фигуры до 22.
`FROZEN_DIGESTS` сдвинулись на всех восьми фигурах, и сдвиг проверен НЕ этим
числом: перевод участников обратно в прямые даёт прежние значения побитово.
Закрыт открытый счёт по `two_holes` — площадь 1408 из 1408, покрытие 180 / 368
против независимого эталона.

**Что стало видно про настоящий частичный источник.** На `bf6` 12 рёбер домена
лежат на 11 классах прямых, законов 3, и каждый из трёх классов с законом несёт
РОВНО ОДНО ребро — то есть коллинеарность к отказу там отношения не имеет, и
старый счёт совпал с новым. Отсюда объём отдельного среза меряется, а не
гадается: девять рёбер — НЕПОДВИЖНЫЕ СТЕНЫ (`SupportLineV1` с `q = 0` это
выражает, `PolygonV1` — нет), и задача размером 9 из 12, а не 3 и не 12.

**Закон сохранения развязан от `RawCoverage`, и круговость привязки измерена.**
Если Policy B не резала ничего (`proofs = ()` — законная ветка), союз вкладов
равен `RawCoverage` тождественно: на `ell` при α = 1 площади 43 и 43, подписи
множеств петель совпадают, диагностик **ноль**, — а квадрат `[5,6]×[5,6]` пуст.
Охранник верен, эталон был не тот. Привязка стала параметром `conservation` со
значением по умолчанию; логика инварианта не тронута ни на строку
(`_loop_set_signature` была общей на обе подписи и до среза), конвейер вызывает
функцию без параметра и ведёт себя как прежде — это проверено сравнением самих
объектов, а не «оба зелёные».

Подстановка эталона ловит дыру там, где привязка молчит:
`INTERACTION_POLICY_B_PARTITION_UNPROVEN` с разностью ровно `Σα²` — `ell` 44
против 43 и 80 против 76, `staircase` 44 против 42, `u_shape` 60 против 58 и 112
против 104. Отрицательный контроль сильный: на выпуклой фигуре множество петель
эталона совпадает с множеством петель `RawCoverage` **побитово**, вплоть до
srepr-строки каждой координаты, поэтому подстановка там не меняет ничего.

**Переводчик петель для эталона оказался дешёвым, и это меняет порядок работ.**
Дорога в `point_key` только канонизация НЕрационального значения (`bf6`: 21 590
вызовов, 44 с). У прямоугольного эталона все координаты рациональны, поэтому его
петли стоят ноль и уже построены (`mitered_inset_loop`). Прежняя оценка про грани
ОЧЕРЕДИ остаётся в силе — там `SqrtSumV1`, — но переводить их и не надо: эталоном
очередь быть не может, это круг. Проверка рационального семейства не ждёт ничего.

Эталон-множество существует не при каждой α, и это названо: вырождение ядра
(`ell`, α = 3), слияние двух рёбер отступа (`double_notch`, уже α = 1) и фигура с
дырами (`hole_1`) дают `INSET_IS_NOT_ONE_LOOP` либо
`INSET_LOOP_NEEDS_A_FIGURE_WITHOUT_HOLES`; ПЛОЩАДЬ эталона при этом честная.

Не поддержаны эталоном и названы исходом `EDGE_IS_NOT_AXIS_PARALLEL`: `comb_2`,
`comb_4` (зубья скошены), `right_triangle`, `diamond`, шесть `star_9_seed_*` и
три `field_building_002_scale_*` — у них митрованный отступ не прямоугольный и в
дробях не считается.

Ворота: ядро **1305** (было 1156), хост **857 passed, 3 skipped**, архитектура
**326**. Под `kernel/src` тронут один модуль — `interactions/policy_b.py`,
и только добавлением параметра со значением по умолчанию. Бюджет
`apply_policy_b` поднят 480 → 487 с записанной причиной.

### Шаг 6 — робастность через ограничение входа, а не через точность числа ✅ ядро

Решение принято по внешнему разбору производительности и проверено замерами.

**Что взято от Boost.Polygon:** робастность покупается *ограничением входа*.
Координаты целые — значит степень предиката ограничена наперёд — значит знак
доказывается дешёвой арифметикой без порогов и без откатов. Именно поэтому
`pyvoronoi` требует `scaling factor`: это не каприз API, а условие работы
фильтра.

**Чего от Boost брать НЕ надо, и это замерено:** трёхуровневого фильтра. В C++
он нужен, потому что там есть выбор между машинным словом и многоразрядным
числом. В Python выбора нет — `int` уже многоразрядный:

| orient2d | время | точность |
|---|---:|---|
| нынешний путь через `ExactScalar` | 9.89 мкс | точно |
| `float` (фильтр в стиле Boost) | 0.30 мкс | только при \|коорд.\| ≤ 2²⁵ |
| **Python `int` на решётке** | **0.25 мкс** | **точно при любой величине** |

`float` не быстрее `int` и при этом ограничен. Фильтр покупать нечем.

**Граница 2²⁵ доказана и тесна.** Из 2b+2 ≤ 53 следует b ≤ 25. Проверено
состязательным корпусом (определитель ровно ±1 при больших координатах —
худший вход): при b=25 расхождений нет, при b=27 их 247 из 3000, при b=40
неверен каждый ответ. Первая версия теста брала случайные точки, показывала
ноль расхождений даже при b=40 и не доказывала ничего: у случайной тройки
определитель большой, и `float` угадывает знак. Константа объявлена для
будущего порта на C++, в Python на корректность не влияет.

**Цена решётки — вершины плавают.** Точка пересечения на решётке в общем
случае не лежит и привязывается к ближайшему узлу. Владелец это разрешил для
декалей. Но плавание записано: `SnappedPointV1` несёт точную невязку дробью,
выход за бюджет — именованный исход `SNAP_DISPLACEMENT_EXCEEDS_BUDGET`.

| | было | стало |
|---|---:|---:|
| orient2d | 9.89 мкс | **0.25 мкс** (40×) |
| point-in-loop, петля 40 сегментов | 692.8 мкс | **17.3 мкс** (40×) |
| расхождений с точным путём | — | **0** из 400 проб |
| порогов и откатов в горячем пути | интервал + символьный | **нет ни одного** |

Сделано ядро (`kernel/src/cftuv_envelope/robust/`, 323 строки с тестами) и
доказано дифференциально против нынешнего пути. **Миграция arrangement на него
не сделана** — это отдельная работа, и она меняет дайджесты: их основанием
станут целые координаты вместо строк `srepr`.

**Решётка даёт константу, а не показатель степени.** 40× не спасает при
O(k^2.15): к 2000 рёбрам это по-прежнему часы. Показатель может опустить
только локализация точки по уже построенной топологии. Нужны оба.

> **Что добавляет полевой замер.** Оба числа шага 6 (orient2d 9.89 мкс,
> point-in-loop 692.8 мкс) сняты на рациональных координатах. На bf6
> `_point_in_loop` стоит **44 мс** на вызов — в 64 раза дороже, потому что там
> значения алгебраические и каждое конструирование идёт через
> `factor(cancel(...))`. Решётка бьёт именно по этому: она убирает
> `ExactScalar` из горячего пути целиком, то есть закрывает найденный в шаге 4
> драйвер, а не только константу предиката. Цена — алгебраическую координату
> на решётку точно не положить, она привязывается к узлу; ровно для этого и
> заведены `SnappedPointV1` и `SNAP_DISPLACEMENT_EXCEEDS_BUDGET`.

### Полевой прогон 2026-07-25 — цена оказалась в радикалах, а не в сканах

`building.002`, 6 цепочек, 7 доменов. Прогон сделан на сборке со счётчиками и
с обеими правками (знак рационального, `tracemalloc`), и он расслаивает
результат надвое.

**Правки сработали — на рациональных доменах.** Против `64cb2e3`:

| стадия | мелкий, было | мелкий, стало | bf6, было | bf6, стало |
|---|---:|---:|---:|---:|
| DOMAIN_CLIP | 22 мс | **7.5–9.4 мс** | 4013 мс | 3891 мс |
| RAW_UNION | 22 мс | **6.3–7.1 мс** | 14433 мс | 14407 мс |
| INTERACTION | 82 мс | **50–57 мс** | 25260 мс | 25441 мс |

Мелкие домены — 2.8–3.2×. bf6 — **ровно ноль**.

**Разделяющая величина — одна.** `ALGEBRAIC_CANONICALIZATIONS`: bf6 = **13600**,
все остальные домены = 0 или 30. Подтверждается вторым счётчиком:
`ARRANGEMENT_MAX_COORDINATE_CHARS` у bf6 = 338 против 14–15 у остальных. 338
символов — это не большое рациональное (оно дало бы ~110), это вложенное
алгебраическое выражение.

**Цена измерена.** `_canonical_expr`: рациональное 0.23 мкс, алгебраическое
**2758 мкс** — в 12 000 раз дороже. 13600 × 2.76 мс = **37.5 с** при полных
43.7 с у bf6. Радикалы — это практически весь bf6.

**Локализация точки — не главный драйвер, вопреки карте и внешнему разбору.**
`POINT_LOCATION_SCANS` у bf6 = 3448 против 84–108, то есть 34×. Время — 2100×.
Раскладка: 34× даёт число сканов, **66× даёт цена одного скана** на
алгебраических координатах. Оба множителя настоящие, второй больше.

**Откуда радикалы.** `unit_g(v) = v / sqrt(dot_g(v, v))` — нормировка нужна,
чтобы поставить точку на метрическом расстоянии α. У мелких патчей метрика
такова, что корень уходит; у bf6 — нет, и радикал протекает в каждую
построенную координату.

**Хирургическая правка проверена и отвергнута.** Тангенс в
`support_segments_for_use` нормируется, хотя локально используется только в
знаковых предикатах и в `owner_normal_g(normalize=False)`. Снятие нормировки
меняет семантику в 8 тестах: `tangent` сохраняется в `SourceSupportSegment` и
его длина покидает функцию, уходя в конструкции. Откачено. Точечно радикал
отсюда не убирается.

**Отсюда следует, что делать.** Радикал берётся при построении координаты на
расстоянии α. На решётке этой координаты нет — есть целый узел, и всё, что
ниже по течению, снова рационально. То есть шаг 6 (`robust/`) и есть лечение
bf6, а не просто константа: 12 000× на операцию, а не 40×.

**И отдельно: bf6 не разрешается не из-за скорости.**
`MULTIWAY_INTERACTION_POLICY_UNPROVEN`, 5 компонент, 9 моделей прихода, 10
кандидатов = C(5,2). При бесконечной скорости bf6 всё равно отказал бы.

Речь про coverage — про столкновение юбок, а не про узлы, где сходятся
ChainUse. Узлы сварены в одну компоненту ещё на компиляции
(`compile_interaction_components`, union-find по `front_component_id` плюс
Strip со своими терминальными Angular/Junction). Взаимодействие бывает только
между несвязанными юбками. Счётчики это показывают прямо: у мелких патчей
`INTERACTION_COMPONENTS = 1`, `CANDIDATES = 0` — поэтому они и разрешаются.

**Причина отказа — отсутствие времени, а не отсутствие правила.** Попарный крой
зависит от порядка: A режет B, потом результат режет C, и ответ меняется, если
начать с A–C. Если события обрабатывать по возрастанию α, порядок задаёт
геометрия, и одновременная встреча k фронтов становится ОДНИМ событием с k
участниками вместо k(k−1)/2 кроёв.

Примитив при этом уже скелетный: `equality_line` строит равенство времён
прихода двух движущихся **прямых** (`n·p = c + s·α`) — это биссектриса straight
skeleton, а не Voronoi. Разрыв между скелетным фундаментом и попарной
надстройкой и есть отказ.

Целевой бэкенд для этого записан в репозитории давно —
`PLANAR_CORNER_SEEDED_SKELETON` (`docs/decal_wavefront_theory_note.md`, §S4),
с событиями collapse/split/meet и прямыми event-loci. Работа не в том, чтобы
дописать правило multiway, а в том, чтобы заменить попарный крой очередью
событий. Это же снимает и показатель степени: события — O(n log n), попарный
крой — нет.

### Шаг 4 — сначала измерить, потом оптимизировать

**Счётчики на месте.** Профиль печатает по каждому домену сводку
масштабирования и полный список счётчиков, тот же набор уходит в
JSON-sidecar. Видны обе стороны arrangement (вход и выход), обе стороны клипа
и объём работы INTERACTION. До этого выход arrangement не считался вовсе, а
`DOMAIN_CLIP` и `INTERACTION` — 55% времени центрального патча — не имели ни
одного счётчика.

Полевые времена, которые надо объяснить (5 рёбер, один запрос):

| стадия | мелкий патч | bf6 | отношение |
|---|---:|---:|---:|
| `DOMAIN_CLIP` | 35 мс | 2128 мс | 60× |
| `RAW_UNION` | 19 мс | 5065 мс | 268× |
| `INTERACTION` | 77 мс | 4595 мс | 60× |

bf6 съедает 11.8 с из 12.4 с всего прогона.

**Драйвер найден, и это не то, чего ждали.** Полевой прогон (`building.002`,
рёбра 1–6, 7 доменов) плюс профиль bf6:

| гипотеза | предсказание | замер | итог |
|---|---|---|---|
| число пересечений k | ∝ времени | 8× против 656× | опровергнута |
| число вкладов | ∝ времени | линейно, 15 вкладов ≈ 1 с | опровергнута |
| длина точных чисел | ∝ времени | 1.6× при вдвое большей длине | опровергнута |
| сканы локализации точки | ≈120 000 | 3 168 (37×против 811×) | опровергнута |
| **канонизация алгебраических значений** | — | **95 с из 158** | **подтверждена** |

Счётчик `ALGEBRAIC_CANONICALIZATIONS` по доменам: у шести мелких патчей **0**,
у bf6 — **21 590**. Это не отношение, а бинарная разница. `_canonical_expr`
даёт 110 770 вызовов и 95 с из 158; алгебраических среди них 15 791, то есть
~6 мс на значение против 0.25 мкс у рационального.

Важно, чего это НЕ значит: интервальный фильтр знака из шага 3 работает —
`exact_sign` уходит в символьный путь 33 раза из 13 347. Течёт не предикат, а
**конструирование**: `ExactScalar.from_value` канонизирует каждое построенное
алгебраическое значение через `factor(cancel(...))`.

Радикалы появляются там, где карта патча не ортонормальна: `unit_g`, `angle_g`
и `length_g` вводят `sqrt`, и он расходится по всем производным точкам. Осевые
патчи остаются рациональными и стоят 130 мс, bf6 — 44 с.

Отсюда следует, что цель шага 4 была выбрана неверно: `_point_in_loop` и
trapezoidal map лечат 3 168 сканов, а не 21 590 канонизаций. Настоящая работа —
**каноническая форма алгебраического значения**, и `DECISIONS.md` уже относит её
к отдельному решению владельца, а не к оптимизации: эта строка идёт в дайджест.

**Замер после слияния шагов 5 и 6.** Прогон на установленном объединённом коде
(`building.002`, рёбра 1–6) против того же прогона до слияния. Результат
расслаивается ровно так, как записано выше по домену:

| стадия | мелкие, было | мелкие, стало | bf6, было | bf6, стало |
|---|---:|---:|---:|---:|
| `DOMAIN_CLIP` | 21–27 мс | **7.7–11.6 мс** | 4334 мс | 4008 мс |
| `RAW_UNION` | 19–25 мс | **6.3–8.0 мс** | 14419 мс | 13834 мс |
| `INTERACTION` | 76–107 мс | **51–60 мс** | 43899 мс | 46669 мс |

Мелкие домены — 2.5–3×, что совпадает с независимым замером выше. bf6 — ноль,
и wall clock тоже (64.1 → 65.7 с), потому что bf6 даёт 98% прогона и перекрывает
собой выигрыш на остальных шести доменах.

Причина расслоения: шаг 5 ускоряет **рациональную** ветку `exact_sign`, а bf6
стоит на алгебраической канонизации; ядро решётки из шага 6 в конвейер ещё не
подключено.

Практический вывод: миграция arrangement на решётку — **единственный из трёх
шагов, который может сдвинуть время bf6**. Но «в поле ноль» было бы неверным
описанием: на рациональных доменах, то есть на большинстве патчей обычного меша,
шаг 5 даёт втрое.

Второе, что вскрыл прогон: bf6 **не разрешается**. `Raw 7/7, Resolved 6/7`,
отказ `INTERACTION_POLICY_B_PARTITION_UNPROVEN` («Policy B clipping did not
reproduce the exact RawCoverage set») после 44 с в INTERACTION. Ускорять
стадию, которая затем выбрасывает результат, — не первая задача.

### Шаг 7 — `COMPILED_ENVELOPE_CACHE_ALPHA_DEPENDENCE`

Без alpha-независимой компиляции интерактивный drag невозможен в принципе,
а это основной сценарий инструмента.

Ворота — исполняемые, а не описательные:

```python
def test_envelope_field_sla():
    # building.002, 36 рёбер, все seam-цепочки: сегодня 870 с
    assert evaluate_all_seam_chains_seconds() <= 30
```

Побочный, дешёвый пункт того же слоя (не блокирует): `PatchSurfaceIR` в
`cftuv/surface_ir.py` пересоздаёт словари-индексы на каждом обращении и сканирует
все грани в `patch_faces`/`patch_triangles`. Замерено O(P·N): 500 патчей / 10 000
граней — 1.6 с только на трёх вызовах. Лечится `cached_property` и предпосчитанными
`faces_by_patch` / `triangles_by_patch`. Тот же антипаттерн уже размножился в
`_PatchSurfaceIdView` (`cftuv/envelope_topology_export.py`).

---

## СОСТОЯНИЕ НА 2026-07-31 — точка входа для новой сессии

Этот раздел заменяет handoff-документ. Правило прежнее: «состояние репозитория
и есть handoff», новых `.md` не заводить (храповик
`test_markdown_document_count_does_not_grow`).

### Цель и где она сейчас

Владелец видит в Blender **покрытие с owner'ами** — движок QUEUE в панели
Envelope Debug: скелет + грани + покрытие очередью, палитра владельцев,
живой alpha на кэше. Веера вогнутых вершин (мягкий угол) считаются на полевых
мешах, включая острые reflex: привязка направлений и **одна bound-геометрия
на оба пути поставлены** (`9eb3825`): sliver сведён в ноль по-компонентно,
очередь равна эталону ТОЧНО даже при стенах, правило прямой вершины (B′)
закрыло главный полевой счёт правил скелета — `building` 98 → **112/122**,
«2» 29 → **32/32**. Видимая митра снята, владелец принял глазами.

**31 июля — четыре полевых среза за день** (принципал вёл через субагентов
Opus 5, записи в DECISIONS за 2026-07-31): ребро-призрак внутри цепи убран
слиянием контура (`4c210eb`, принят владельцем); бесконечный счёт d4 заменён
именованным исчерпанием по бюджету точной работы (`aa6448b`); atlas переведён
на по-кусковое CF-перечисление (`3ae5929`) — 328 с/724 МиБ → 3.7 с/7 МиБ,
**полевой d4 `building` 98.2 с ≤ 120 — зелёный**; near-planar: два дефекта
(вырождение плоскости кадра + осевой снап ломает наклонную планарность),
принят маршрут C″ (ветка `a65c258`), **слияние `9d6dbd2` В ПРИЁМКЕ**: ядро
1935 моим прогоном, хост+архитектура НЕ добежали (сбой инструмента) — первый
шаг новой сессии.

### Что работает, числами (все — сегодняшний полевой прогон/фикстуры)

| | |
|---|---|
| `building.002`, все швы, QUEUE | **13/13** за 4.884 с (кап 5); patch 0 = `EXACT`: 4 привязанных веера, 0 деградаций |
| остаток очереди над эталоном | `full_selection` и `point_contact`: **доказанный ноль** (одна bound-геометрия; атрибуция «стены — остаток» опрокинута); `weighted_normals`: ноль |
| меш «2» | **32/32** за 21.237 с = old29 1.446 (кап 3) + new3 19.791 (кап 27; цена трёх бывших отказников); SLA-кап total 30 |
| `building` (458 швов) | **112/122** за 102.23 с (кап 120); было 99, паритетная норма 102 пройдена правилом прямой вершины |
| свип приёмки (`tools/blender_field_sweep.py`) | **26/26 FINISHED** на всех 13 мешах (перемерено на `9eb3825`) |
| ворота на `9eb3825` | ядро **1830**, репозиторий **913 + 3 skipped** (в т.ч. архитектура **350**) — все три перемерены принципалом |

### Следующие срезы, в порядке приоритета

0. **ПОЛЕВАЯ ПРИЁМКА near-planar C″** (слияние `9d6dbd2` принято тремя
   воротами принципала — ядро **1935**, хост **1011+3**, архитектура **362** —
   ff в `t8qz7s` = `3787ad3`, установлено со штампом 23:07): остались полевые
   проверки: наклонный домен владельца (`building.002`, швы
   11/23/25/26/38/39 → ждём EXACT), крыша `building.004` (4/8 METRIC_REJECTED
   → ждём проход), пересчёт near-planar отказов d4, **финальный свип
   78 кнопок** → поставка Density с глазами владельца. Вопросы исполнителю
   отвечены (C″ утверждён, знак нормали, история сохранена) — см. DECISIONS.
1. ~~**Карточка `LATTICE_SNAP_BREAKS_A_VERTEX_FAN`**~~ — ЗАКРЫТА
   (MIRRORED-CHART-FAN-01). Гипотеза «веер знаменателя 1711 не переживает
   привязку якоря» ОПРОВЕРГНУТА: снап невиновен, знаменатели ни при чём.
   Причина — порядок опор веера: рецепт направлений строит его в обходе
   патча-ВЛАДЕЛЬЦА, а полигон очереди требует обхода петли ВЫЧИСЛЕНИЯ, и на
   зеркальной карте (`COORDINATE_CW_MATCHES_OWNER_PATCH`) это противоположные
   смыслы. `building_patch10_density4_v1` дошёл до `EXACT` (скелет 20 узлов,
   22 грани, 4 рациональных веера) без единой правки решётки. Остаётся ПОЛЕВОЙ
   пересчёт класса: 7 доменов d4 `building` + 1 домен «2» — прогнать и
   записать новые исходы.
2. **DISSOLVE-STRAIGHT-01** — растворение прямых вершин (владелец одобрил как
   оптимизацию): предикат силуэта — точный нулевой поворот; стоп-лист якорей
   подсчётом ссылок по снапшоту; ~64 растворимых вершины по сцене (замер);
   evaluation-закон V3 печатью тега; полная A-пропорция гейтов.
3. **Spike CGAL + Surfer2** (двухбэкендовый, одобрен владельцем): те же
   фикстуры, проекция топологии/владельцев против нашего валидатора, skeleton
   ≤ 50 мс; SVCGAL — третья точка; STALGO — письмом Хельду при попадании.
4. **Product-карточка ручки плотности Density 0..4** (принято владельцем:
   d=0..4, default 1; контракт — вариант A-narrowed: 5 значений
   `MaxSubturnValueId` π/2..π/6, UI — целочисленный enum).
   **Пол решён владельцем: вариант A** —
   E = max(2, C), H = max(1, C−1), q = d+2; норма «360° → 4 hidden на
   default» снята (H на default ограничен 2, плотности 2..4 дают H до 5).
   Пол входит только НОВОЙ именованной policy-властью селекции; явный
   V1/60° — byte-stable. Границы — ceiling lower-OPEN/upper-CLOSED, интервал
   через порог — fail-closed `ANGULAR_PROFILE_SELECTION_UNCERTAIN`.
   **Proof-only Gate DENS-A′ закрыт** тремя принятыми линиями:
   AUTHORITY `0a05e51`, RADICAL `8d7d735`, SPLIT-STATIC `69424b7`.
   Следующий срез — именно product-карточка. Следствие названо: новый default
   меняет пологие reflex — новые базлайны, приёмка глазами, сверка
   UX-номенклатуры с bevel.
2. **Отдельная product-карточка static/wrapper cache-key.** Сегодня ответы
   верны побитово в обоих порядках переиспользования, но контракт ложен:
   кэш-ключ хоста адресует alpha-зависимые байты полной обёртки вместо
   alpha-free static-подготовки. Раскол static/wrapper провести отдельно от
   Density-карточки, включая хост-шов `envelope_debug_session.py:430-462`.
3. **Остаток правил скелета** — 10/122 нерешённых на `building` (после
   правила прямой вершины; имена исходов — по-доменной таблицей из
   SEM-серии). Экспорт фикстур отказников — обёрткой, Blender у
   принципала/интегратора.
4. **Отображение деградации хостом** (чип владельцу выставлен, агент не
   запускался — по решению владельца делает новая сессия): счётчик
   `CONVEYOR_DEGRADED_MITER_CORNERS` и по-вершинные
   `PreparedRegionV1.degraded_miter_corners` провести в `_region_findings`
   (`cftuv/envelope_queue_export.py`), sidecar и слой по образцу
   `ENV_93_QUEUE_REFUSED` — владелец должен видеть, КАКОЙ угол острый и почему.
5. Палитра владельцев: 141 сверх восьми слотов на «2» — нужна нормальная
   раскраска. Near-planar в полевых прогонах (кривые меши отказывают метрикой,
   политика есть у аддона). Смена эталона `policy_b` и замена продакшн-пути
   (равенство очереди и эталона уже доказано на меше без стен). Затем —
   производительность крупных доменов и решение о C++ (ядро извлекаемо по
   построению).

### Открытые счета, названные и не закрытые

| счёт | число |
|---|---|
| `building` нерешённые | 10/122, именованные исходы в таблице SEM-серии; приоритет 3 |
| `building.002` близко к капу | 4.884 при капе 5 — запас 2.4 %; следить в расписках |
| запаркованная certified prime-authority | ответы доказанно равны; building 39.3 против 50.8 с; переоткрывать только при ужесточении бюджета building (Gate I: NO SELECTOR) |
| кэш-ключ хоста адресует alpha-зависимую обёртку | ответы сейчас верны побитово по теореме переиспользования; контракт ложен — отдельная product-карточка static/wrapper, хост-шов `envelope_debug_session.py:430-462` |
| `NO_RULE_MEETING_NOT_RECONNECTABLE` / `NO_RULE_JOINT_IS_CODIRECTIONAL...` | 8 на корпусе; прежняя полевая цена «15 доменов» опрокинута B′; оставшийся полевой счёт входит в именованные 10/122 `building` по таблице SEM |
| полевой пересчёт бывшего `LATTICE_SNAP_BREAKS_A_VERTEX_FAN` | причина найдена и снята (порядок опор на зеркальной карте, MIRRORED-CHART-FAN-01); фикстурный домен patch10 d4 = `EXACT`. НЕ ПЕРЕМЕРЕНО В ПОЛЕ: 7 доменов d4 `building` + 1 домен «2» — Blender за принципалом |
| граница memo `_density_exact_memo.subturns` | 1249 байт/единицу без вытеснения; давление умерло с CF (724→7 МиБ) — наблюдение, не карточка |
| устаревшие профили скоупов гейта | `one_chain_l0`/`three_chains_l0` ссылаются на номера рёбер, которых нет на отредактированной сцене; приёмочный скоуп — `all_seam_chains_l0` |
| ветка `a84f4c` отстала на `c975b7a` | force-move git отказывает (где-то checked out); решить: живое зеркало или списать |
| exit 1 сводки `field_cycle_summary` | штатный сигнал «есть именованные отказы», НЕ провал ворот — вердикт по расписке (gate_failure/performance) |
| у веера нет `envelope_instance_id` | `conveyor.py:1048` перебирает только Strip-спеки |
| `AMBIGUOUS_OWNER_SPANS` | 4 на bf6-full (владелец в классе коллинеарных рёбер не определён по входу) |
| литералы `expected_kernel_results` экспортёра | описывают байты до пересборки; `fixture_id` в манифесте full_selection — копипаста |
| течь monkeypatch в `test_reference_envelopes.py` | обход стоит в `test_wavefront_conveyor.py`, корень жив (чип у владельца) |
| `resolve_coverage_interactions` | пропускает `ExactArrangementRotationSystemUnproven` без имени |
| R2 — целочисленные предикаты в `arrangement.py` | заблокирован привязкой конструкций |
| смена alpha на всём `building` | 3.17 с на 104 домена — не realtime; drag одного домена ~30 мс |

### Метод и его цена: ОДИННАДЦАТЬ ошибок карточек

Работа срезами: карточка → исполнитель (Opus 5, отдельная ветка,
инкрементальный коммит-и-пуш) → НЕЗАВИСИМАЯ проверка чисел принципалом →
слияние. За серию исполнители, владелец, гейты и аудит нашли одиннадцать
ошибок в карточках принципала; все в `DECISIONS.md` (27–30 июля): 6–9 — за
28-е, 10 — за 29-е, 11 — за 30-е:

6. перечень затронутых тестов объявлен полным по grep — полным его объявляет
   только прогон;
7. знаменатели ворот взяты из прежнего замера, а не перемерены на момент
   выдачи (и «без изменений» недостижимо там, где храповик растёт по
   построению);
8. механизм пересказан по памяти («срез хорды» вместо двухзвенной цепи) при
   том, что код лежал в той же карточке;
9. предсказанный разведкой отказ (NGON, одноцепочечные петли) не попал ни в
   карточку, ни в явный счёт — и его нашёл владелец кнопкой;
10. критерий «остаток → ноль» был недостижим по построению: причина
    атрибутирована агрегирующими тождествами, которые замыкаются при любом
    распределении причин; атрибуция в критерии — только по-компонентная
    (запись 2026-07-29 в DECISIONS.md);
11. закон привязки задан без имени метрики (два разных канонических k, оба
    точные) и с универсальной границей там, где власть по-вершинная; поймано
    гейтом A″ до кода — закон обязан именовать метрику и власть каждой
    границы (запись 2026-07-30).

Правила, оплаченные этими ошибками, — в записях; главное новое: **приёмка —
это кнопка владельца**: `tools/blender_field_sweep.py` (настоящий оператор на
каждом меше сцены) обязателен перед каждой поставкой.

### Полевые условия

Blender и сцена доступны: фоновые прогоны — `tools/field_cycle.bat`
(`-ObjectName`, `-Engines queue`, свежесть выхода гарантирована) и свип;
живой сеанс — порт 9876 (`tools/blender_remote.py`; MCP-мост таймаутит).
Установка — `tools/install_to_blender.bat` **из основного чекаута
`E:\GITHUB\CFTUV`** (выровнен с веткой сессии; после каждого слияния делать
ff и переустановку) — пишет ШТАМП, и панель аддона показывает его. Ядро на
машине гонять с `PYTHONSAFEPATH=1`. Тестовый меш ступени ~100 — «2» (бывш.
`walls.001`, переименован владельцем). Сцена в репозиторий не кладётся.
`MITER`/`BEVEL` как режимы не возвращаются; хорда-BEVEL — только
отрицательный контроль в тестах.

---

## Фаза 3 — паритет возможностей

Один чеклист вместо карт и handoff-документов. Строка = возможность легаси = тест =
галочка владельца в Blender.

Отменённая этой фразой система — `docs/agent_execution/envelope_v1/`, 47 файлов
с мастер-планом, протоколом агента, 33 карточками и шаблонами handoff — **удалена
2026-07-27**. Она лежала рядом с отменяющим её текстом и ничем не была помечена,
а её шаблоны приглашали проблему вернуться. История в git осталась. Рост числа
документов остановлен исполняемым храповиком
(`test_markdown_document_count_does_not_grow`), потому что бюджет обязательного
чтения меряет один файл, а разрастание пошло по другим.

Таблица пуста не по недосмотру: она заполняется из `ACCEPTANCE.md`, а тот
намеренно узок — владелец вносит кейсы по мере усложнения, а не декларирует
авансом (Фаза 1). Заполнять её нечем до тех пор, пока очередь событий не считает
покрытие в конвейере; сегодня она не подключена, и препятствий к подключению
четыре, они перечислены ниже в разделе про очередь.

| Возможность | Легаси | Envelope | Тест | Проверено в Blender |
|---|---|---|---|---|
| _заполняется из ACCEPTANCE.md по мере готовности_ | | | | |

Также в этой фазе решается судьба модулей, на которые ссылаются только тесты и
никакой продакшн-код: `cftuv/decal_distance_witness.py`,
`cftuv/decal_chart_parametrization.py`. Оба — часть замораживаемого legacy-конвейера.

---

## Фаза 4 — удаление легаси

Одним коммитом, чтобы откат был одной командой:
`decal_voronoi.py`, `decal_rails.py`, `decal_rail_geometry.py`, `decal_charts.py`,
`decal_atlas.py`, `decal_chart_*`, `decal_corner_model.py`,
`decal_distance_witness.py`, их тесты, зависимость `pyvoronoi` и routing-слой в
`decals.py`. Ориентировочно ~1.1 МБ кода и ~250 тестов.

Записи в `test_architecture.py` (`FROZEN_LEGACY_ENGINES`, `MODULE_LINE_ALLOWANCE`,
`FUNCTION_LINE_ALLOWANCE`) удаляются вместе с файлами — тест сам подскажет, что
осталось.

---

## Что удерживает от повторения

Всё исполняемое, всё установлено **до** начала работ, а не после:

| Бюджет | Значение | Что предотвращает |
|---|---|---|
| Размер модуля | ≤ 2000 строк | `decal_voronoi.py` = 16 889 |
| Размер функции | ≤ 120 строк | `_m1_surface_arrangement` = 1815 |
| Обязательное чтение | ≤ 150 строк | было 2705 в шести файлах |
| Модули-сироты | 0 | `band_operator.py` |
| Крупные бинарники | не растут (было 67, стало 6) | 76 МБ устаревших скриншотов |
| Красные тесты в чистом клоне | 0 | 4 × `atlas_frozen` |
| Полевой SLA | ≤ 30 с | было 870 с, стало 24 с *(Фаза 2, достигнуто)* |
| Число документов | не растёт (было 148, стало 100) | 47 файлов отменённой системы карт |

И два процессных правила:

1. **Состояние репозитория и есть handoff.** Никаких `docs(...): record ... handoff`
   коммитов. Если следующая сессия не понимает состояние из тестов, `DECISIONS.md`
   и этого файла — чинится код, а не пишется документ.
2. **В зоне легаси новый модуль появляется только вместе с удалённым.**
